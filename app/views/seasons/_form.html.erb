<%= form_with(model: season, local: true) do |form| %>
  <% if season.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(season.errors.count, "error") %> prohibited this season from being saved:</h2>

      <ul>
      <% season.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :season_number %>
    <%= form.text_field :season_number, id: :season_season_number %>
  </div>

  <div class="field">
    <%= form.label :premiere_date %>
    <%= form.date_select :premiere_date, id: :season_premiere_date %>
  </div>

  <div class="field">
    <%= form.label :end_date %>
    <%= form.date_select :end_date, id: :season_end_date %>
  </div>

  <div class="field">
    <%= form.label :summary %>
    <%= form.text_field :summary, id: :season_summary %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
